<?php

return [
    /*
     | Ключ IndexNow. Любая строка 8–128 символов из [a-zA-Z0-9-]. Тот же ключ
     | должен лежать в файле public/<key>.txt — так поисковик подтверждает,
     | что адреса подаёт владелец сайта. Файл создаётся командой
     | `php artisan indexnow:key`.
     |
     | Пустой ключ полностью выключает отправку: на локальной машине и в тестах
     | ходить в поисковик незачем.
     */
    'key' => env('INDEXNOW_KEY', ''),

    /*
     | Точка приёма. Яндекс и Bing используют общий протокол и обмениваются
     | поданными адресами между собой, поэтому одной отправки достаточно.
     */
    'endpoint' => env('INDEXNOW_ENDPOINT', 'https://yandex.com/indexnow'),

    /*
     | Хост, который подаётся в запросе и от которого считаются адреса.
     | По умолчанию берётся из APP_URL.
     */
    'host' => env('INDEXNOW_HOST'),

    // За один запрос протокол разрешает не более 10 000 адресов.
    'batch' => 10000,

    'timeout' => 15,
];
